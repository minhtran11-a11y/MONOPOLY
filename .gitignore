# Dependencies
node_modules/

# Build output & coverage
dist/
coverage/

# Environment (Supabase keys etc.)
.env
.env.*
!.env.example
# .env.production is committed on purpose: it carries only the public-by-design
# Supabase URL + anon key that ship inside the client bundle anyway (RLS protects data).
!.env.production

# Generated reports & test artifacts
lighthouse-report.json
*.log
npm-debug.log*

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings
.claude/settings.local.json

# GAN harness screenshots (regenerable artifacts)
gan-harness/shots/

.vercel
